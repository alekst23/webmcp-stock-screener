# T-0001-5/T-0001-8: base URL of the deployed FastAPI backend, used by the
# fetch-based ResearchEngine implementation (ApiClientConfig.baseUrl).
# Local dev default:
PUBLIC_API_BASE_URL=http://localhost:8000
